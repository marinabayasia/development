#include <stdio.h>
#include "util.h"

int main(void) {

    printf("hello, Marina\n")
    // int sz = 100;

    // int a[sz]

    // int age = 0;

    // printf("Put your age here: ");
    // scanf("%d", age);
    // printf("Your age is %i ", age);
    // getchar();
    // //multiplyUntil(0, sz);
    // for (int i = 0; i < sz; ++i) {
    //      a++ = multiplyNumberBySelf(i);
    //      printInteger(i);
    //  }
}


